module.exports = {
  theme: {
    extend: {},
    container: {
      center: true
    }
  },
  variants: {
    borderWidth: ['responsive', 'hover'],
    borderColor: ['responsive', 'hover', 'focus', 'active', 'group-hover'],
  },
  plugins: [
  ],
}
